import { render, screen } from '@testing-library/react';
import AccountForm from './index';

// xdescribe('AccountForm', () => {
// at least associate, active
// at least associate, past due
// at least associate, delinquent
// at least associate, inactive
// at least associate, resigned
// at least associate, removed
// guest, active
// guest, inactive
// guest, limited
// guest, locked
// emeritus
// deceased
// at least associate
// it('should ');
// });
//
